[project]
name = "bili-notify-ai-worker"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = ["ai/v1"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
